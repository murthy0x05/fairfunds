# Incomplete Tasks

Based on the requirements in the assignment and the current state of the application (`fairfunds.vercel.app`), the following tasks are incomplete. While the backend logic (API actions and services) for these features appears to exist in the codebase, the **frontend user interfaces** are missing from the website:

- [x] **Manage Group Memberships (Completed)**: You've added the UI to add new members with a join date. We have now also added the UI to remove members and mark them as having left the group.
- [x] **Create Expenses Manually**: Created an "Add Expense" form/UI. 
- [x] **Record Payments / Settle Debts Manually**: Created a UI to manually record settlements or payments between users.
